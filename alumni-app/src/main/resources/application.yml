server:
  port: 80
spring:
  cache:
    type: redis
  redis:
    host: ${DB_REDIS_HOSTNAME:redis}
  data:
    mongodb:
      database: app
      port: 27017
      host: ${DB_MONGO_HOSTNAME:mongo}
  rabbitmq:
    host: ${RABBIT_HOSTNAME:rabbit}
    username: guest
    password: guest
logging:
  config: classpath:logback.xml
